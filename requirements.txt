yt-dlp>=2024.12.13
faster-whisper>=1.0.3
ffmpeg-python>=0.2.0

google-api-python-client>=2.150.0
google-auth-oauthlib>=1.2.1
google-auth-httplib2>=0.2.0

requests>=2.32.0
tenacity>=9.0.0
loguru>=0.7.2
pyyaml>=6.0.2
pydantic>=2.9.0
python-dotenv>=1.0.1

# tzdata is required on Windows so zoneinfo (Python 3.11+ stdlib) can resolve
# IANA names like "Asia/Singapore". On Linux/macOS the system tz database is
# usually available, but pip-shipping tzdata makes the project portable.
tzdata>=2024.1

imagehash>=4.3.1
Pillow>=11.0.0
better-profanity>=0.7.0

# audio fingerprinting (chromaprint binary must be installed system-wide; pip pkg is the python bindings)
pyacoustid>=1.3.0

# Kling AI video generation (Pivot.6)
PyJWT>=2.8.0

# RSS topic ingest (Pivot.6)
feedparser>=6.0.0

# Narration (Pivot.6 Edge; Pivot.7 Kokoro primary)
edge-tts>=6.1.9
kokoro>=0.9.0
soundfile>=0.12.1

# Image sourcing (Pivot.7)
ddgs>=9.0.0

# Dashboard (read-only local viewer)
fastapi>=0.115.0
uvicorn>=0.32.0

# dev / testing
pytest>=8.0.0
httpx>=0.27.0
